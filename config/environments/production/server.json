{
  "host": "${process.env.DATABASE_HOST || 'localhost'}",
  "port": "${process.env.DATABASE_PORT || 1337}",
  "proxy": {
    "enabled": false
  },
  "autoReload": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false
  }
}
